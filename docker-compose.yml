version: "3"

services:
  
  backend:
    build: ./server
    container_name: backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI:${MONGO_URI}  
  
  frontend:
    build: ./client
    container_name: frontend
    ports:
      - "3000:80"

    